VITE_API_BASE_URL=http://localhost:8000/api/

# Dev-only: shows a raw transitions-JSON panel next to each log sheet, to
# validate the grid/remarks against the exact data driving them.
VITE_SHOW_DEBUG_PANEL=false
